# 研究限制（LIMITATIONS）

> **本文件是交付門檻的一部分。** 任何引用本專案結果的論文、簡報或摘要都必須同時
> 引用本文件（PRD §7.4）。限制與結果同步更新。

最後更新：2026-09-05

---

## 1. 宇宙選樣偏誤（第一條，無法消除）

本研究的 267 檔清單**不是指數、不是市值前 267 大、不具市場代表性**。選樣規則是
「每個 TWSE 產業別依證券代號升冪取前 10 檔」——由於台股代號大致依上市先後編配，
這等於「每個產業中上市年資最深的 10 檔」，是一個**成立年份偏老、規模偏中小**的樣本。

所有推論的母體只能寫成「本樣本涵蓋之 267 檔個股」，**不得寫成「台股」或「台灣上市
公司」**（PRD §1.4、§11）。

**已確認的缺口**

| 缺口 | 內容 |
|---|---|
| 金控完全缺席 | 金融 10 檔全為銀行、票券、產險。富邦金 2881、國泰金 2882、中信金 2891 等**皆不在內**。金融業結論僅適用銀行與保險 |
| 主要權值股缺席 | 聯發科 2454、廣達 2382、日月光投控 3711、華碩 2357、中華電 2412、台灣大 3045 均不在宇宙內 |
| 未涵蓋產業 | 建材營造、電腦及週邊設備、通信網路、其他 |
| 外國企業 | 含 11 檔 -KY（會計科目、稅制與 MOPS 揭露格式與本國企業不同） |

**宇宙層級的 look-ahead（無法完全消除）**：這 267 檔是以 2026 年的產業分類、且以
2026 年仍存在（或曾被爬到）為條件選出的。對 2015 年的投資人而言，這個清單不可知。
因此**所有投資組合結果一律標示為 ex-post universe，不得宣稱為可實作策略**。
穩健性檢定 R13 提供「僅含樣本起始日前已上市個股」的對照。

---

## 2. 樣本期間短於 PRD 規劃

| 項目 | PRD 規劃 | 實得 |
|---|---|---|
| PTT 封存 | — | **2015-04-27 ~ 2025-01-24** |
| 主樣本 | — | **2015-05-01 ~ 2024-12-31**（第一個至最後一個完整月份） |
| 價格回補 | 2011 起 | 2014-01-01 起（FinMind） |
| Yahoo 新聞 | 2011–2026 | **未取得**（見 §4） |

PRD §3.1、§3.8、§4.1 多處以「2011–2026」為前提撰寫，實際可得的注意力資料只有
**2015-05 起的 116 個月**。連帶影響：

- **漲跌幅 7% → 10%（2015-06-01）的制度斷點幾乎全落在樣本外**：7% 期間在主樣本內
  僅約 4 週，PRD §5.11 第 6 項的「7% 期間 vs. 10% 期間」對照**在本樣本不可行**，
  已在 `output/tables/T12_robustness.csv` 明確標記為 SKIPPED 並註明原因。
- PRD §3.9 的「2015-06 前的截斷更嚴重，須單獨跑子期間」同樣不可行。

## 2.1 有效個股數為 260 而非 267

7 檔的 TWSE 上市日晚於 PTT 封存結束日，因此在主樣本中**沒有任何一列觀測**：

中傑-KY 6965（2025-03-07）、寶綠特-KY 6887（2025-03-10）、兆聯實業 6944（2025-05-28）、
微程式 7721（2025-07-29）、中華資安 7765（2025-09-08）、資拓宏宇 6614（2025-11-25）、
世紀風電 2072（2026-03-26）。

面板為 **260 檔 × 506 週的非平衡面板**；上市前的週維持缺列（不補零），符合 PRD §3.1。

---

## 3. 推文沒有作者與時戳 → `low_effort` 的定義被迫改寫

既有 PTT 封存的 `pushes` 欄位是**純字串陣列**，只有推文內容，**沒有作者、沒有逐則
時戳**。全樣本共 11,029,966 則推文無法定位到時間窗口。

依 PRD §3.3 的明文備案，`low_effort` 一律定義為 **「不含推文」**，且**不得以文章時戳
推估推文時間**。本專案採用此備案。

**後果**：`low_effort` 實際上只涵蓋 `[新聞]`、`[閒聊]`、`[情報]`、`[公告]` 等被動轉貼
類文章，而 PRD 理論設計中 low_effort 的「主要組成」（推文）完全缺席。H2 的分層對比
因此偏向「文章類型」而非「投入深度」，解讀時必須說明這一點。

## 3.1 文章分類清單與 PRD 規格不符

PRD §3.3 的 `CATEGORIES = "標的|新聞|請益|心得|閒聊|公告|問卷|其他"` 與實得語料不符：

- **缺「情報」**——實測佔約 11%，是第四大分類，若照 PRD 原 regex 會全部落入未分類。
- 另缺 `[創作]`、`[爆卦]`、`[投顧]`、`[討論]`。
- PRD 列出的 `[問卷]` 在語料中**不存在**。

本專案已擴充分類清單並將「情報」歸入 `low_effort`（情報多為消息轉貼）。此歸類是
研究者判斷，不是 PRD 規格，列為變數定義的偏離。

---

## 4. Yahoo 新聞管道未執行 → 主表控制項缺漏

依專案決策，Yahoo 新聞**僅在 PTT 結果不佳時才啟用**，本輪未抓取。後果有三：

1. **`news_count` 缺席**。PRD §4.2 明寫此控制為「主表必要項，不是 robustness」，
   用來排除「週末 PTT 關注度只是週末新聞的反射」。**缺此控制，H1 的資訊解讀存在
   未被排除的替代假說。**
2. PRD §5.9 的跨管道比較（需求端 vs. 供給端）**未執行**。PRD 稱此節「應寫成論文的
   一個獨立小節」，目前缺席。
3. PRD §5.11 第 14 項穩健性檢定不可行，已標記 SKIPPED。

---

## 5. 授權資料未取得 → 所有結果為 diagnostic

下列控制變數需 TEJ 或等價授權，目前**未取得**：

| 變數 | 狀態 | PRD 對應 |
|---|---|---|
| `analyst_count`、`forecast_dispersion`、`forecast_revision` | 未取得 | §3.8、§4.5 |
| 台灣四因子週報酬（`TWN/AFACTORW`） | 未取得 | §4.1 |
| 無風險利率 | 未取得（因子表無 rf 欄位） | §4.5 |
| `selling_expense_to_sales`（推銷費用強度） | 未建置 | §3.8、§4.5 |
| `news_sentiment`、`news_impact` | 未建置 | §4.5 |

依 PRD §3.8 的不可妥協缺值政策，這些欄位**維持缺欄位，未補零也未以公開替代變數
暗中頂替**。因此：

> **本輪所有迴歸與投資組合結果一律標記為 `diagnostic`，不得稱為主結果或初步結論**
> （PRD §11）。`data/processed/analysis_readiness.csv` 的 `formal_main_return` 為 False。

分析函式在診斷模式下會剔除涵蓋率不足的控制變數並將被剔除的欄位記入該列的 `note`；
正式模式（`is_diagnostic=False`）則在欄位不齊時直接中止，不降級輸出。

**分析師覆蓋**：PRD §3.8 已預期本宇宙多數個股無券商覆蓋，改採 `has_analyst_coverage`
二元變數。目前連二元變數也無來源，該項完全缺席。

---

## 6. PRD 的雙市場假設與實得清單不符

PRD §3.1、§4.1、§4.5、§5.11-3 與風險 R3 均假設宇宙「同時包含上市（TWSE）與上櫃
（TPEx）個股」，並將市場別解析列為 P0 阻斷性前置作業。

**實際解析結果：267 檔全數為 TWSE 上市，TPEx 0 檔**（來源：TWSE `t187ap03_L` 與
TPEx `mopsfin_t187ap03_O`，逐檔比對，未解析者 0 檔；證據見
`audit/venue_resolution_report.csv`）。

因此下列 PRD 工作項**不適用**並已標記：

- `venue_field_map.yaml`（兩市場欄位對應）
- TPEx 端點、仟股單位換算與其回歸測試
- §5.11 第 3 項「含／不含上櫃個股」穩健性對照（已標記 SKIPPED 並註明原因）
- 風險 R3

**但轉板歷史仍是未解決的細節**：部分個股（例如世紀風電 2072）在 TWSE 上市前曾於
上櫃／興櫃交易，FinMind 的價格資料涵蓋該段期間，而 `market_venue.csv` 目前只記錄
TWSE 區間。這對主樣本影響極小（相關個股多在樣本期後才上市），但不應宣稱
`market_venue.csv` 已完整涵蓋轉板紀錄。

---

## 7. 補班日自然實驗（H4）檢定力嚴重不足

由實際交易日曆推導，全期間（2014–2025）的補班星期六**僅 9 天**，落在主樣本內的
**只有 8 個事件週**，且**最後一次是 2018-12-22**——2019 年起台灣不再安排補班交易日。

依 PRD §5.10，有效群集數等於**事件週數**（8）而非觀測數，且週固定效果會吸收補班週的
共同成分。因此：

- H4 的結果**只能定位為探索性訊號，不得作為結論**（PRD §5.10 備案 c）。
- 已依 PRD 要求以 wild cluster bootstrap（群集於週層級）報告，並輸出
  `output/tables/T10_makeup_feasibility.csv` 記錄事件週數與可偵測效果量。
- PRD §5.10 宣稱「個股數由 50 增至 267 使 H4 從『可能無法識別』變成『有機會識別』」
  ——橫斷面觀測數確實增加，但**群集數沒有**，所以這個樂觀評估並不成立。

---

## 8. 測度與資料品質

### 8.1 名稱碰撞與通用詞降級

267 檔中**194 檔（73%）為兩字簡稱**，誤配風險遠高於 v1.0 的 50 檔樣本。已實作
PRD §3.4 的最長匹配 ＋ 碰撞群組 ＋ 通用詞降級，並將 51 檔列入只用代號比對。

**降級不是免費的**：只用代號比對會系統性降低該股的關注度水準。若降級名單與規模或
產業相關，會對 H6 造成偏誤。`is_code_only_matched` 已進入面板，§5.11 第 2 項提供
含／不含降級股的對照。

配對結果中 **70% 來自代號比對**（最可靠），16% 為需上下文的簡稱比對，14% 為一般簡稱。

### 8.2 大量清單型貼文（PRD 未預期，實測是最大的單一測度威脅）

覆蓋熱圖揭露 2018–19 有一條「幾乎所有個股同時有討論」的假垂直帶。追查後確認來自
**大量清單型貼文**：「加權股價指數成分股暨市值比重」「非擔任主管職務之全時員工薪資
資訊」「本週小小程式選股」等，一篇文列出數十至 **242 檔**代號。

**2,513 篇（佔有配對文章的 3.7%）貢獻了 78,424 個配對列，佔全部配對的 42.7%。**
這些是資料傾印，不是對任一個股的關注。

處置：依 PRD §4.3 的精神**標記而非靜默刪除**——新增 `is_bulk_listing` 欄位
（門檻 `max_tickers_per_article = 15`，寫入 `config/settings.yaml`），主規格排除，
並以 `scripts/sensitivity_bulk_listing.py` 做門檻敏感度測試（穩健性 R15）。

> **這個門檻會改變定性結論，必須在論文中明確揭露：**
>
> | 門檻 | 保留配對 | β(週末) | t | β(週間) | t |
> |---|---|---|---|---|---|
> | ≤5 | 48.0% | 0.000286 | 1.10 | −0.000020 | −0.11 |
> | ≤10 | 54.5% | 0.000319 | 1.28 | 0.000062 | 0.32 |
> | **≤15（主規格）** | **57.3%** | **0.000409** | **1.61** | **0.000106** | **0.55** |
> | ≤20 | 63.2% | 0.000300 | 1.28 | 0.000083 | 0.46 |
> | ≤30 | 83.9% | 0.000223 | 0.96 | 0.000306 | 1.87 |
> | 不過濾 | 100% | 0.000270 | 1.04 | 0.000338 | 1.86 |
>
> 不過濾時「週間關注度較有預測力」——這與原論文相反，但那是**清單型貼文多在平日
> 張貼**所造成的假象。過濾後型態翻轉為週末 > 週間（與 H1 方向一致），但兩者在任一
> 門檻下都**未達 5% 顯著**。翻轉點落在門檻 20 與 30 之間。

### 8.3 交易活動共動篩檢會刷掉標的

PRD §3.10 第 2 項的 `Attention ~ Turnover + |Return|` 篩檢，在長尾個股上會刷掉不少
標的。依 PRD 要求，**刷掉本身就是結果，已完整報告於
`audit/keyword_screening_report.csv`，未靜默剔除**。

### 8.4 權值還原

FinMind `TaiwanStockPrice` 為**未還原**收盤價。本專案以 TWSE 除權除息計算結果表
（TWT49U）的「除權息前收盤價 / 除權息參考價」自行後向還原，涵蓋 2014-02 ~ 2025-12
共 9,815 個事件。方法與逐檔涵蓋率記於 `audit/price_adjustment_report.csv`。

此還原**未經第三方（例如 TEJ 還原股價）逐檔驗證**，僅通過內部一致性檢查。PRD §3.6
要求「未通過此驗證前，任何報酬結果一律標記為 diagnostic」——本輪結果本就全部標記
為 diagnostic，此項不改變結論。

### 8.5 外資及陸資持股比率只是 proxy

公開資料只有「外資及陸資持股比率」，**不是全體機構持股**。命名一律使用
`foreign_holding_pct`，不得寫成 `institutional_holding`（PRD §11）。

此變數由 TWSE MI_QFIIS 每 5 個交易日抽樣取得（低頻變數，週內幾乎不變），以前向
填補至日頻。發行股數同源，市值與周轉率因此繼承相同的抽樣頻率限制。

### 8.6 產業分類的 look-ahead

PRD §3.9 要求 `sector_as_of_2023`（主規格）與 `sector_pit`（逐年當期分類，穩健性）
並存。**目前只有 2026 年的現況分類**，逐年分類快照未取得，因此：

- 主規格使用現況分類（相當於 `sector_as_of_2023` 的角色）。
- §5.11 第 10 項的 `sector_pit` 對照**未執行**，已標記 SKIPPED。
- 運動休閒、綠能環保、居家生活、數位雲端為 2023 年新設類別，把它們套回 2015 是
  **分類上的 look-ahead**，H8 的產業層級結論須保留此但書。

---

## 9. 稀疏性

主樣本 123,828 個 ticker-week 中，**76.6% 的關注度為零**（排除大量清單型貼文後）。
稀疏度分層（trailing 52 週）：

| tier | ticker-week 數 | 眾數為該層的個股 | 曾進入該層的個股 |
|---|---|---|---|
| `dense`（非零 ≥40 週） | 6,920 | 13 | **38** |
| `sparse`（非零 4–39 週） | 78,802 | 178 | 257 |
| `silent`（非零 ≤3 週） | 37,871 | 69 | 256 |

**PRD §8.2 的門檻（`dense` ≥ 40 檔）未通過：實得 38 檔。** 決定可識別性的是「曾進入
dense 估計樣本的個股數」（38），不是眾數分層（13）；兩者都報告於
`audit/sparsity_distribution.csv`。

依 PRD §4.2 與 §8.2，未達門檻**應觸發 Yahoo 新聞備案決策**。本輪依專案決策暫緩
Yahoo 抓取，因此 H2 的認知投入分層結果（T4，13,734 個觀測、全部係數不顯著）
**檢定力不足，不得解讀為「high_effort 沒有預測力」**——只能說本樣本無法識別。

依 PRD §3.2.1，主迴歸限定 `dense` ＋ `sparse`；`silent` 保留在面板中但自連續型主規格
排除，改以起始事件設計（H7）處理。

**H2 的認知投入分層只在 `dense` 子樣本上可識別**——`[標的]` 原創分析文對長尾個股一年
可能不到一篇。這一點在論文中必須先講，而不是在結果不顯著時才解釋（PRD §3.2.1 第 3 點）。

---

## 10. 其他外部效度限制

- **PTT 使用率 2020 年後下滑**，使用者組成偏向年輕男性與科技類股投資人。個股與週
  固定效果吸收部分影響，§5.11 第 12 項提供子期間穩定性檢定，但**跨族群外推無效**。
- **關注度測度是發文數，不是搜尋量**。一律稱為「異常 PTT 關注度」，不得寫成 ASVI／
  SVI／搜尋量（PRD §11）。
- **非三大法人訂單失衡的殘差包含未列入三大法人的大戶與其他機構**，只能稱為
  「非三大法人訂單失衡」，不得改寫為 retail order imbalance 或散戶淨買超（PRD §11）。
- **Google Trends 未納入**。本宇宙的長尾個股在 Google Trends 上多半低於回報門檻，
  PRD §3.10 已載明僅可能用於 `dense` 子樣本，全樣本不可行。

---

## 11. 命名紀律對照表（PRD §11）

| 正確用語 | 不可用 |
|---|---|
| 本樣本涵蓋之 267 檔個股（有效 260 檔） | 台股、台灣上市公司、台股代表性樣本 |
| 非三大法人訂單失衡 | 散戶訂單失衡、retail order imbalance |
| 異常 PTT 關注度 | ASVI、SVI、搜尋量 |
| 銀行與保險業子樣本 | 金融業 |
| 外資及陸資持股比率 proxy | 機構持股比例 |
| ex-post universe 之機械年化多空價差 | 年化績效、可投資報酬、策略績效 |
| 成本前多空價差 | 多空報酬 |
| diagnostic 結果 | 主結果、初步結論 |
