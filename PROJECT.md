# 研究設計與變數定義（單一真相來源）

本文件是變數定義的權威來源。程式碼與本文件不一致時，以本文件為準並修正程式碼。
研究**目標**規格見 `PRD.md`；已知偏離見 `LIMITATIONS.md`。

---

## 1. 樣本

| 項目 | 規格 |
|---|---|
| 研究宇宙 | 267 檔固定清單，橫跨 28 個 TWSE 產業別 |
| 選樣規則 | 每產業依證券代號升冪取前 10 檔；不足者取全部（玻璃 5、水泥 7、造紙 7、油電燃氣 8） |
| 市場別 | **全數 TWSE 上市**（逐檔解析，TPEx 0 檔） |
| 觀測單位 | ticker × 週（`ticker, week` 唯一） |
| 週的定義 | 星期日錨定（`W-SUN`），週標籤為該週的**星期日** |
| 主樣本期間 | 2015-05-01 ~ 2024-12-31（PTT 封存的完整月份） |
| 有效個股 | **260 檔**（7 檔上市日晚於樣本結束） |
| 面板規模 | 123,828 ticker-week × 260 檔 × 506 週，**非平衡** |

**非平衡的處理**：上市前的週**維持缺列**——不補零關注度、不補零報酬。
`listing_date` 為必要欄位，來源為 TWSE `t187ap03_L` 的「上市日期」。

**時間對齊**：特徵週 `w` 的關注度 → 報酬週 `w+1`（下一個日曆週的週一開盤至週五
收盤）。週六／日的關注度落在以該週日結尾的週，因此**嚴格領先**次週報酬。
下一個日曆週若完全休市，該筆維持缺值，**不得跳過休市週去取更後面那一週的報酬**。

---

## 2. 關注度變數

主指標為**異常 PTT 關注度**。語彙上不得寫成 ASVI／SVI，因為量化的是發文數而非搜尋量。

```
Attention_{i,w} = 該週指派給股票 i 的 PTT 文章數（依窗口切分）
AbnAtt_{i,w}    = log1p(Att_{i,w}) − mean(log1p(Att_{i,w-8..w-1}))
```

回顧窗 8 週，`min_periods = 8`，**嚴格不含當期**。不足 8 週維持缺值。

### 2.1 兩套窗口（都必須產出，不可互相取代）

| 欄位 | 切法 | 定義 |
|---|---|---|
| `abn_attention_all` | — | 整週 |
| `abn_attention_weekday` | 日曆（主規格） | 週一至週五全天 |
| `abn_attention_weekend` | 日曆（主規格） | 週六、週日全天。**主要自變數** |
| `abn_attention_intraday` | 交易時段（延伸） | 實際交易日的 09:00–13:30 |
| `abn_attention_non_trading` | 交易時段（延伸） | 其餘所有時間（含平日夜間與連假） |

**時段判定不可用星期幾**。台灣春節、連假、颱風假頻繁，寫死「週五 13:30 → 週一
09:00」會系統性低估非交易窗口。一律以**實際交易日曆**（由實際成交日推導）判定：
若週一為國定假日，該日全天與週二開盤前仍屬非交易時段。

### 2.2 零值處理

PTT 的零是**真實的零**（該週確實無人討論），與 Google Trends 的 0（低於回報門檻，
是遺漏值）性質相反。因此：

- PTT 情境用 `log1p`，保留零。
- 正值限定指標（零視為缺值）保留為 robustness：`abn_attention_*_posonly`。

### 2.3 稀疏度（本專案最關鍵的新規格）

主樣本 **79.7% 的 ticker-week 關注度為零**（已排除大量清單型貼文並完成歸屬修正）。若不處理，「連續 9 週皆為零」的長尾股與
「關注度剛好等於常態水準」的台積電，`AbnAtt` 都等於 0，但經濟意義完全不同。

| 欄位 | 定義 |
|---|---|
| `att_nonzero_weeks_52` | 過去 52 週中關注度非零的週數（不含當期） |
| `att_mean_level_52` | 過去 52 週的平均文章數（**水準**，非異常值） |
| `sparsity_tier` | `dense`（≥40）／`sparse`（4–39）／`silent`（≤3） |
| `is_initiation` | `Att_w > 0` 且 `sum(Att_{w-8..w-1}) == 0` |
| `att_lookback_all_zero` | 回顧窗 8 週全為零（此時 `AbnAtt` 不具比較意義） |
| `att_zero_base` | 上述旗標的虛擬變數，**必須進入迴歸吸收**，不得當成 AbnAtt = 0 |

**估計規則**

1. 主迴歸樣本限定 `sparsity_tier ∈ {dense, sparse}`；`silent` 保留在面板中但自
   連續型主規格排除，改以事件設計（H7）處理。
2. H2 的認知投入分層**只在 `dense` 子樣本上可識別**。
3. 三個 tier 的門檻寫死於 `config/settings.yaml`，並在穩健性中做敏感度測試——
   **不得作為篩選後才決定的自由度**。

---

## 3. 認知投入分層

| 分層 | 內容 |
|---|---|
| `high_effort` | `[標的]` **原PO**（需論述與理由） |
| `mid_effort` | `[請益]`、`[心得]`、`[標的]` 回文 |
| `low_effort` | `[新聞]`、`[閒聊]`、`[情報]`、`[公告]`、`[爆卦]`、`[投顧]` |

標題解析容許 `Re:`／`Fw:` 前綴（回文佔股板貼文大宗），原PO 與回文分開記錄。

**兩項對 PRD 的偏離**（見 LIMITATIONS §3）：

1. **推文不進入任何分層**。封存的推文無作者、無時戳，依 PRD §3.3 的明文備案，
   `low_effort` 定義為「不含推文」，且不得以文章時戳推估推文時間。
2. **分類清單依實得語料擴充**：PRD 原清單缺「情報」（佔約 11%），另缺創作、爆卦、
   投顧；PRD 列出的「問卷」在語料中不存在。

---

## 4. 個股歸屬

267 檔中 **194 檔（73%）為兩字簡稱**，名稱空間遠比 50 檔擁擠。比對流程依序：

1. **證券代號**：`\b\d{4}\b` 比對宇宙清單，加上下文檢查排除年份、價格、數量、日期。
   - 代號區間與西元年份重疊（中鋼 2002、春源 2010、春雨 2012、中鋼構 2013、
     中鴻 2014、豐興 2015…）。這些代號**必須有正面證據**才採計：同文出現該股簡稱，
     或代號後緊接股票語境詞。否則「2015 年的行情」會被算成豐興的關注度。
2. **最長匹配優先**：變體依字元長度降冪排列，命中後從文本「消耗」該片段。
3. **碰撞群組**：群組內先試長名、再試短名；短名命中額外要求上下文證據。
4. **通用詞降級**：名稱本身為高頻常用詞者，**停用簡稱比對，只用代號**（51 檔）。
5. 一篇文可對應多檔，記為多筆 ticker-article 配對（同文多列）。

**碰撞群組**（十組）：統一系、台塑系、中鋼系、東和系（**完全前綴且跨產業，最高
風險**）、三商系、永豐系、華新系、中化系、台亞／台聚、長榮系。長榮改用「長榮海運」
比對，避免長榮大學／航空。

**降級不是免費的**：只用代號會系統性降低該股的關注度水準。若降級名單與規模相關，
會對 H6 造成偏誤。`is_code_only_matched` 進入面板，穩健性做含／不含對照。

**實得配對**：251,858 篇 → 62,888 篇有配對（25.0%）→ 171,073 個 ticker-article 配對。
其中 70.6% 來自代號比對，15.1% 為一般簡稱，14.2% 為需上下文的簡稱。

**歸屬正確率 95%**（抽驗修正後）。除 PRD §3.4 的四道流程外，另需三項 PRD 未預期
的規則，全部由抽驗實證導出（見 `audit/adjudication/README.md`）：

6. **阻擋延伸**：命中片段被相鄰字（可夾空白）擴成另一實體時不採計
   （統一→統一證券／投信；中華電→華電；平台聚集→台聚）。
7. **發文樣板移除**：PTT `[標的]` 格式範例「(例 2330 台積電)」不算關注度。
8. **排行表欄位偵測**：買賣超金額／張數欄的四位數不是證券代號。

**大量清單型貼文須排除**：指數成分股表、薪資表、程式選股輸出等一次列出數十至 242 檔
代號的貼文，是資料傾印而非對個股的關注。門檻 `max_tickers_per_article = 15`
（寫入 `config/settings.yaml`），標記 `is_bulk_listing` 而非刪除；主規格排除
78,424 列（42.7%）。**此門檻會改變定性結論**，敏感度測試見 LIMITATIONS §8.2。

---

## 5. 應變數與機制變數

| 變數 | 定義 |
|---|---|
| `ret_next` | 下一個日曆週的週一開盤至週五收盤報酬（**還原權值後**） |
| `non_inst_roi_next` | 次週非三大法人訂單失衡 |
| `turnover_next` | 次週異常周轉率（以流通在外股數標準化） |

**基準報酬**：主規格以**週固定效果**吸收所有市場層級共同衝擊，應變數用原始週報酬。
267 檔長尾樣本的等權報酬不是市場報酬，而是一個小型股傾斜的組合，因此不採用
v1.0 的「事前扣除等權市場報酬」。

**權值還原**：FinMind 價格為未還原，須以**兩張** TWSE 表後向還原（每個交易日乘上其
後所有事件因子的累積乘積）：

| 事件 | 來源 | 因子 |
|---|---|---|
| 除權、除息 | 除權除息計算結果表 TWT49U（9,815 筆） | `參考價 / 前收盤價`，**< 1** |
| **減資** | 股票減資恢復買賣參考價格 TWTAUU（394 筆） | `恢復買賣參考價 / 停止買賣前收盤價`，**> 1** |

**減資不可省略**：股本縮減使股價機械性跳升，漏掉會產生 +109% 這種定義上不可能的
日報酬（台股漲跌幅上限 10%）。

**第三方驗證**：以既有 Yahoo 快取獨立檢核（`src/market/validate_prices.py`）。
Yahoo 收盤價**已還原股本變動但未還原股利**，與本專案互補，因此非除權息日的日報酬
應幾乎相等、除權息日應系統性分歧。上市後 **255/259 檔通過**。

### 5.1 非三大法人訂單失衡

```
r_buy  = 總成交股數 − 三大法人買進股數
r_sell = 總成交股數 − 三大法人賣出股數
ROI    = (r_buy − r_sell) / (r_buy + r_sell)     分母為 0 時缺值
```

**三個輸入一律為股數**。三大法人 = 外資（含外資自營商）＋ 投信 ＋ 自營商（自行
買賣 ＋ 避險）。

**必須避免的量綱錯誤**：(a) 用「總成交 − 三大法人**買賣超**」把總額與淨額相減；
(b) 用**成交金額**去減**股數**。回歸測試對兩者都設有守門斷言。

**低成交量的分母問題**：長尾個股常有單日成交量極小的情形，`denom` 極小時 ROI 會爆到
±1 並產生假的極端值。規則：日成交股數 < 10,000 股的交易日自週聚合排除；一週有效
交易日不足 3 天者維持缺值。門檻寫入設定檔並做敏感度測試。

**命名紀律**：殘差包含未列入三大法人的大戶與其他機構，只能稱為「非三大法人訂單
失衡」，不得寫成 retail order imbalance 或散戶淨買超。

---

## 6. 分析規格

- 連續變數**在稀疏度分層內標準化**，避免長尾零值把大型股的變異壓扁。
- **個股固定效果 ＋ 週固定效果**為主規格。
- **標準誤同時 cluster 至個股與週**（Petersen 2009）——267 檔同時暴露於相同的週別
  市場衝擊，橫斷面相依性是一階問題。
- 樣本量或識別條件不足的模型寫入表格並標記 `SKIPPED`，**不得勉強輸出係數**。
- 每張表報告觀測數、個股數、涵蓋週數，以及進入該規格的稀疏度分層組成。

### 6.1 主迴歸（H1）

```
Ret_{i,t+1} = α + β₁·AbnAtt_weekday + β₂·AbnAtt_weekend
              + δ·att_zero_base + γ'X + firm_FE + week_FE + ε
```

四個規格：僅整週／僅週間／僅週末／兩者同時，另加交易時段切法一組。

### 6.2 機制（H3a/H3b）

以 `non_inst_roi_next` 與 `turnover_next` 取代應變數，各加落後應變數。

| 結果 | 支持的管道 |
|---|---|
| 週間 → ROI 顯著；週末 → Turnover 顯著 | 資訊處理（同原論文） |
| 週末 → ROI 顯著 | 價格壓力（台灣的邊界條件） |

### 6.3 異質性 ＋ 反轉（H6 ＋ H5，核心貢獻）

調節變數逐一代入：`log(att_mean_level_52)`、`log(market_cap)`、`turnover`、
`amihud`、`foreign_holding_pct`。預期交互項 β₂ 顯著為負。

> **判讀必須聯立。** 單獨的負交互項**不能**區分兩條管道——資訊處理與價格壓力都
> 預測低覆蓋股效果更強。決定性的證據是低覆蓋股的效果**是否在後續週反轉**：
> 反轉 → 價格壓力；不反轉 → 資訊處理。本節與反轉檢定**合併為同一張表**呈現
> （`T6_joint_reading.csv`），不得分開報告後各自解讀。

### 6.4 起始事件（H7）

對 `sparse` 與 `silent` 個股，連續型 `AbnAtt` 沒有意義（回顧窗全零時恆為 0），
改用「由零轉正」的事件設計，並以事件研究法報告 t−4 到 t+8 週的 CAR 路徑，
**含事前四週的平行趨勢檢查**。

**平行趨勢在本樣本未通過**（τ=−2 的 t = 2.34），因此主規格為**同週匹配對照的
DiD 事件研究**：

- 對照取自**同一週**的未起始個股——市場共同衝擊自動差分掉，等同週固定效果。
- 匹配變數：τ=−1、τ=−2 的異常報酬**分開進入**（只用累積量會殘留事前趨勢），
  加 τ=−4..−3 累積、`log(市值)`、`log(周轉率)`。
- **caliper**：事前報酬差異須 ≤ 1%，否則該事件無可用對照而剔除。SMD < 0.1 的
  慣例門檻在數千個事件下不足以保證平衡。
- 標準誤群集於**事件週**。

判讀規則寫死於 `matched_events._verdict()`：事前仍顯著 → 無法識別；事前清除且
事後顯著 → 支持 H7；事前清除且事後不顯著 → H7 不成立。

### 6.5 投資組合（升格為主結果之一）

依前週週末關注度分五等分，每組約 53 檔。**必要欄位**：

- 等權與市值加權各一次，再依市值中位數雙重排序。
- **成本後報酬**：手續費 0.1425%×2（打 6 折）＋ 證交稅 0.3%（賣出）＋ 滑價 20bp×2。
  **不含成本的多空價差不得作為主要結論陳述。**
- **可交易性篩選後版本**：剔除週成交金額低於門檻者。篩選前後的差距本身就是結果。
- 一律標示 **ex-post universe**；「每週平均 × 52」只是機械年化，不可解讀為可投資績效。

---

## 7. diagnostic 與正式結果的分離

正式主表要求 `config` 定義的控制變數**全部齊備**；缺一則估計函式直接中止，
不降級輸出。診斷模式下會剔除涵蓋率不足的控制變數，並把被剔除的欄位記入該列的
`note` 欄，讓讀者知道這組係數少了什麼。

`data/processed/analysis_readiness.csv` 的 `formal_main_return` 為權威旗標。
目前為 **False**（授權資料未取得），因此**所有結果均為 diagnostic**。
