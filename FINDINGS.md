# 首輪結果（全部為 diagnostic）

> **所有數字皆為 `diagnostic`**：授權控制變數未取得，`news_count` 缺席，
> `analysis_readiness.csv` 的 `formal_main_return` 為 False。依 PRD §11，
> 不得稱為主結果或初步結論。閱讀本文件前請先讀 [`LIMITATIONS.md`](LIMITATIONS.md)。

樣本：260 檔 × 506 週（2015-05-03 ~ 2025-01-05），123,828 個 ticker-week，
主迴歸樣本 82,079 個觀測（限定 `dense` ＋ `sparse`）。

報酬已還原除權息**與減資**，並經既有 Yahoo 快取獨立驗證（上市後 255/259 檔通過，
見 LIMITATIONS §8.4）。`price_adjustment_verified` = True。

---

## 1. 測度效度：型態幾乎完全複製原論文

| 檢定 | 本研究 | 原論文 |
|---|---|---|
| `corr(整週, 週間)` | **0.938** | 0.90 |
| `corr(整週, 週末)` | **0.400** | 0.40 |

這是本輪最強的證據：儘管測度換成 PTT 發文數（不是 Google 搜尋量）、市場換成台灣、
樣本換成長尾，**兩個窗口與整週關注度的相關性型態幾乎逐位數重現**。它支持「週末關注度
確實是一個與週間關注度不同的建構」，也就是原論文設計的前提。

---

## 2. H1（週末效果）：方向對，強度不足

主迴歸（雙向固定效果，個股與週雙重 cluster）：

| 規格 | 變數 | 係數 | t |
|---|---|---|---|
| 僅週末 | `abn_attention_weekend` | 0.000417 | 1.62 |
| 僅週間 | `abn_attention_weekday` | 0.000140 | 0.71 |
| 兩者同時 | `abn_attention_weekend` | **0.000409** | **1.61** |
| 兩者同時 | `abn_attention_weekday` | 0.000106 | 0.55 |

**週末係數約為週間的 4 倍且為正**，方向與 H1 一致；但 t = 1.61，**未達 5% 顯著**。
交易時段切法（intraday / non_trading）同樣不顯著。

> **這個結論高度依賴一個 PRD 未預期的測度決策。** 若不過濾大量清單型貼文，型態會
> 翻轉成「週間顯著、週末不顯著」——與原論文相反。詳見 LIMITATIONS §8.2 的門檻
> 敏感度表。任何引用本結果的敘述都必須同時交代這個門檻。

---

## 3. 機制檢定：最清楚的一組結果

| 應變數 | 週間關注度 | 週末關注度 |
|---|---|---|
| 非三大法人訂單失衡 `non_inst_roi_next` | **−0.00201（t = −3.23）** | 0.00008（t = 0.16） |
| 異常周轉率 `turnover_next` | **0.00467（t = 8.43）** | **0.00242（t = 5.20）** |

三點解讀：

1. **兩個窗口的關注度都強烈預測次週異常周轉率**，週間效果約為週末的兩倍。關注度
   確實被交易進市場。
2. **只有週間關注度預測訂單失衡，而且是負的**——週間關注度上升，次週非三大法人
   呈現淨賣壓。這不是價格壓力管道預期的「散戶追買推升價格」。
3. **週末關注度對訂單失衡完全沒有預測力（t = 0.16）**，卻顯著預測周轉率。

依 PRD §5.3 的判讀表，「週末 → Turnover 顯著、週末 → ROI 不顯著」落在**資訊處理管道**
那一格。但由於 H1 本身未達顯著，這個機制證據**懸空**——沒有需要被解釋的報酬效果。

---

## 4. H6 跨截面異質性：不成立

交互項 `AbnAtt_weekend × M`（預期顯著為負）：

| 調節變數 M | 交互項係數 | t |
|---|---|---|
| `log(att_mean_level_52)` | +0.000111 | 0.86 |
| `log(market_cap)` | +0.000191 | 1.17 |
| `turnover` | +0.000401 | **1.97** |
| `amihud` | −0.000134 | −1.06 |
| `foreign_holding_pct` | −0.000128 | −0.78 |

**沒有任何一個交互項顯著為負**；周轉率的交互項顯著為**正**（方向與 H6 相反）。
分三組（tercile）估計亦看不到單調型態。

`output/tables/T6_joint_reading.csv` 的聯立判讀結論：

> 主效果本身不顯著，H6 與 H5 皆不具判讀力——兩條管道都無法被本樣本區分。

這是誠實的結論，也是 PRD §2.3 明確允許的結果：「本專案的成功標準不是複製出顯著結果，
而是用足以區分兩個管道的證據回答問題」。本輪的答案是：**證據不足以區分**。

---

## 5. H7 關注度起始事件：最值得追下去的一組

事件研究（476 個起始事件，`sparse` ＋ `silent` 子樣本）的 CAR 路徑：

| τ | −2 | −1 | **0** | +1 | +4 | +8 |
|---|---|---|---|---|---|---|
| 每週異常報酬 | +0.34%（t=2.34） | +0.25% | **+0.87%（t=3.69）** | +0.12% | +0.08% | −0.36%（t=−2.39） |
| 累積 CAR | +0.50% | +0.75% | **+1.61%** | +1.74% | +1.45% | +0.58% |

型態：**事件當週有明顯正報酬，t+1 達到 CAR 高點 +1.74%，隨後單調衰退至 +0.58%
——約三分之二的漲幅在八週內回吐。** 這是價格壓力管道的典型型態。

**但有兩個嚴重的但書**：

1. **平行趨勢檢查未通過**：τ = −2 的異常報酬已顯著為正（t = 2.34）。關注度起始
   不是外生的——價格先動，討論才出現。這是反向因果的直接證據，事件設計的識別
   假設不成立。
2. **τ = 0 是同期，不是預測**。真正的預測性檢定是 τ = +1，其係數不顯著（t = 0.78）；
   事件迴歸的 `is_initiation_weekend` 亦不顯著（t = 0.22）。

因此 H7 目前**只是一個描述性型態**，不構成識別。要救它需要處理反向因果——例如以
事前報酬匹配對照組，或改用「非報酬驅動的關注度起始」子樣本。

---

## 6. H8 產業外溢：不顯著

同產業其他個股的週末關注度對本股次週報酬：係數 0.000197、t = 0.50。產業內相對
關注度亦不顯著（t = 1.15）。t+2、t+4 的反轉檢定同樣不顯著。

---

## 7. H4 補班日：檢定力不足，不可解讀

全期間補班星期六**僅 9 天**，落在主樣本內 **8 個事件週**，且 **2018-12-22 之後再無**。
有效群集數 = 8，需要約 3.2 個群集標準誤才能偵測到效果。

wild cluster bootstrap（群集於週層級，999 次）：交互項 −0.00123，**p = 0.149**。

依 PRD §5.10 備案 (c)，此結果**只能定位為探索性訊號**。PRD 原本預期「個股數增至 267
使 H4 有機會識別」——橫斷面觀測數確實增加，但**群集數沒有**，所以該樂觀評估不成立。

---

## 8. 投資組合：成本吃光價差（R8 完全實現）

五等分多空，每組約 50 檔：

| 規格 | 週數 | 成本前週價差 | t | 實際週換手 | 週成本 | 成本後週價差 | t |
|---|---|---|---|---|---|---|---|
| 等權・未篩選 | 492 | +0.069% | 1.62 | 33.0% | 0.574% | **−0.506%** | −11.8 |
| 等權・可交易性篩選後 | 487 | +0.098% | 1.96 | 39.7% | 0.692% | **−0.594%** | −11.7 |
| 市值加權・未篩選 | 492 | −0.003% | −0.03 | 33.0% | 0.574% | −0.577% | −5.8 |
| 雙重排序・小型股 | 487 | +0.030% | 0.55 | 23.5% | 0.409% | −0.379% | −6.7 |
| 雙重排序・大型股 | 487 | +0.080% | 1.40 | 43.3% | 0.755% | −0.675% | −11.8 |

成本假設：手續費 0.1425%×0.6（雙邊）＋ 證交稅 0.3%（賣出）＋ 滑價 20bp（雙邊），
**按實際換手率計**（33–43%），非假設每週全額換手。

**成本前最好的規格（等權・可交易性篩選後）週價差 0.098%、t = 1.96，剛好擦到顯著；
但成本是它的 7 倍。** 依 PRD §5.7，不含成本的多空價差不得作為主要結論陳述——
本專案的結論是：**這個訊號在扣除長尾個股的實際交易成本後不具經濟意義。**

一律標示 **ex-post universe**；可排序週不連續，「每週平均 × 52」只是機械年化。

---

## 9. 驗收門檻：17 項中 15 項通過

未通過的兩項：

| 門檻 | 要求 | 實得 |
|---|---|---|
| `dense` 檔數 | ≥ 40 檔（否則 H2 無法識別，須觸發 Yahoo 備案） | **38** |
| 正式主表可產出 | `formal_controls_available` = True | **False** |

第一項是 PRD §4.2／§8.2 明訂的 **Yahoo 新聞備案觸發條件**，目前差 2 檔。
第二項需要 TEJ 授權資料。

---

## 10. 下一步建議（依邊際價值排序）

1. **重抓 Yahoo 新聞（必須含逐則發布時戳）**。現有的 `data/raw/yahoo/*/news/` 不可用：
   `time` 欄位 12,365 列**全空**，且是「最新 100 則」快照而非歷史封存（見
   LIMITATIONS §4）。重抓後可同時解三個問題：(a) `news_count` 是 PRD 明訂的主表必要
   控制項，缺它 H1 的資訊解讀有未排除的替代假說；(b) `dense` 檔數差 2 檔即達門檻；
   (c) §5.9 的需求端 vs. 供給端比較是 PRD 認定應獨立成節的貢獻，且**在 H1 不顯著時
   特別有價值**——若週末效應在 PTT 與新聞上表現不同，那本身就是發現。
2. **處理 H7 的反向因果**。事前兩週已顯著的正報酬是目前最具體的識別威脅，也是
   CAR 型態能否從「描述」升格為「證據」的關鍵。
3. **人工抽驗**。`audit/ptt_manual_review_sample.csv` 已備妥 510 篇分層樣本待填。
   PRD §8.1 要求歸屬正確率 > 90%，目前尚未驗證。
4. **取得 TEJ 授權資料**，讓正式主表得以產出。
